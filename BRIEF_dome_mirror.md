# BRIEF — ★87 돔 거울 확장 구현 (A급·Fable 전용)

> ★86 결정 세션(2026.07.29)의 산출물. **다음 세션 첫 메시지에 이 파일 하나로 몰아준다**(A급 세션 수칙).
> 결정은 전부 봉합됨 — 이 브리프에 없는 형태 판단이 나오면 멈추고 현도에게 묻는다.

## 0. 한 줄 정의
현행 "엎어진 잔" 돔(리브 y 0→960)을 림 평면(y=0)에서 **아래로 거울 확장**해
닫힌 방추형 캡슐(y −960~+960, 노브로 가변)로 만든다. **미러 대상 = 경선 리브 72기뿐.**
지면(Ground) 폐기 → 접지 요소들은 **각자 임시 판**(블록아웃)을 받는다.

## 1. 확정 결정 (현도 2026.07.29 — 재론 금지)
1. **리브 = 한 몸.** 리브 #k가 적도를 관통해 아래로 이어진다. 72기 유지(144 아님),
   각 리브 길이 2배. §1 LOCKED "72기 동일" 무손상 — 미러가 72기 전부에 동일 적용되므로.
2. **아래 끝(y=−960) = 빈 개구.** r168, 아무것도 안 놓는다(렌즈·캡 없음).
3. **미러 대상 = 경선 리브뿐.** 건축물(방·드럼·잉카·회랑·테라스…)은 미러하지 않는다.
   ⇒ 수술 자국(문 5·절단·빛기둥 구멍·등불 관·나팔 아가리) 유령 복제 문제 원천 소멸 —
   미러는 **수술 전 민짜 관**의 연장이다.
4. **이번 산출물 범위 = 껍질 + 각자 바닥까지.** 지지 조형(공중 부양 건물을 받치는 표현)은
   추후 건축 어휘로 점진 해결 — 이번에 만들지 않는다.
5. **각자 바닥 = Claude 임시 판**(현도 위임). 각 접지 요소의 발자국만큼의 최소 블록아웃.
   형태·재질은 잠정(Claude 임의값 태그) — 조형 판정은 현도 로컬 순회.
6. **walkable 낙하 문제 = 나중**(장부만: 구 Ground가 walkable이었다 — P3 충돌 보행 전 재론).

## 2. 기하 스펙
- **프로파일:** 하반부 r(−u) = rOf(u), y(−u) = −H·u·MIR_DEPTH_F. 상반부 rOf·H·SCALE **무수정**.
- **노브 `MIR_DEPTH_F`** (신설, 기본 1.0): 하반부 깊이 = H×f. 길이감 판정용 — 현도 로컬 왕복.
  참고: 1.0=깊이960(전체1920) / 0.5=480(1440) / 0.31=300(어깨에서 끝, 굴뚝0).
  ⚠어떤 f에서도 접합 매끈 — u=0 접선 수직 이탈 dr/dy=1.75e-10 실측(★86), 꺾임각 2e-8°.
- **구현 접근(제1후보):** `makeRibCurve`(ribGeometry.js 단일 정본)의 정의역을 u∈[−f,1]로
  연장(표본점 앞에 미러 점 삽입). 리브 셸·문·절단·등불이 전부 이 곡선에서 파생되므로
  한 곳만 고치면 72기가 같이 자란다(§1 LOCKED의 실질). ⚠단 상반부 파생물의 u/f-매개변수
  가정(0=문, 1=나선끝 등)이 깨지지 않게 — **연장은 곡선 객체 수준에서, 기존 소비자에게는
  u 재매핑으로 투명하게**. 구현 전 소비자 13곳 전수 실측 필수.
- **camera far 3000 > 캡슐 대각 2005 ✓** (f=1 기준). fog·조명은 P2 몫 — 건드리지 않는다.

## 3. 접지 명단 → 임시 판 (결정 5의 실행)
- **1단계(측정):** 전 컴포넌트 y≤0 정점 전수 스캔으로 '접지 명단' 자동 추출.
  예상 후보(★86 사전 조사): Ground(폐기 대상 그 자체) · 잉카 계단 하부절단면 ·
  넥서스+날4 밑곡선 · 방사 플랫폼 받침 기둥 · 오벨리스크 기둥 밑끝(RIB_NEWEL_Y0=0) ·
  톱니 슬래브(접지 전고) · 드럼 하강로 권역 하부 · 폴(RIB_POLE_ON=false — 꺼져 있으면 제외).
- **2단계(블록아웃):** 명단 각 항에 발자국 크기 임시 판. 두께는 §2-D ③ 위계 존중
  (판떼기 0.0 금지 — ㊿ 전례). 명단 소진을 검사로 박는다(아래 §5).
- ⚠**Ground 폐기 순서:** 판이 전부 선 뒤에 지운다(봉인 차분 — 파괴적 연산 전 검사).

## 4. LOCKED 판정 (★86에서 선판정 완료 — 구현 중 재확인만)
- **§1 (72기 동일·얇음·S자·불투명):** 무손상 — 미러 72기 동일 적용, 바깥면 정점·법선
  동일성은 기존 R7 [320][321]이 재는 것을 하반부까지 연장.
- **예외 #1(문5)·#2(절단)·#4(빛기둥):** 무손상 — 전부 상반부 국소 수술이고 민짜 미러라
  하반부 유령 없음. ⚠수술 브러시(슬래브·박스)의 y범위가 y<0을 침범하지 않는지 실측 필수
  (무한/과대 브러시가 하반부를 깎으면 유령이 생긴다).
- **불가시 검사(예외#1 조건③ 24점 광선 등):** 시점 좌표 불변이므로 통과 예상 —
  단 하반부 껍질이 새 가림/새 노출을 만들 수 있어 **전 항 재실행으로 확인**.
- **시야 차단 3중·공개(나팔):** 무손상 예상 — 하반부는 관 밖 세계가 아니라 같은 내부.

## 5. 검증 신설 (기존 1044항 green 유지 + α)
1. 접합 C1: 적도에서 상·하 접선 각차 < ε(실측 기준 2e-8° 재확인).
2. 하반부 대칭: 표본 u에서 r(−u)=r(u) · 하반부 72기 방위 격자 동일.
3. 접지 명단 소진: 스캔이 찾은 모든 y≤0 접지 요소에 판이 있다(Ground 제외).
4. 유령 부재: 하반부 껍질에 개구·절단 0(민짜) — 광선 or 정점 검사.
5. 기존 스위트 7종 전 항 green + 빌드 green + check_render(신규 컴포넌트 TARGETS 추가).

## 6. 무드·감성 (현도 지향 — 조형 판단의 나침반, Claude는 제안 금지 유지)
고대 사원 · 버려진 · 숭고함 · 우주선 · 리미널 스페이스 · 신비로움 · 호기심 · 기하학적 ·
브루탈리즘 · 중세 성 · 미래지향. 참조 심상: 몽생미셸 · 천공의 성 라퓨타.
철학 근거: **바닥이 보이면 '끝'을 연상한다 — 기댈 곳은 이 공간뿐이어야 하고 그것이 곧
실체다**(1p8 무한 · 1p15 내재성). 중앙 하부 공간 = 추후 1p16~36(사물의 생산) 예정지
(이번 방학 범위 외 — 공간만 확보).

## 7. 킵(이번 범위 아님 — DESIGN.md §7 창고에도 등재)
리브 틈새 외부 가시 ↔ 1p15 내재성 긴장. 현도 후보: ① 틈을 벽으로 막기
② 클라인병식 안팎 무구분 개편. 복잡 — 나중 결정.
