# Ethica Architectonics — 프로젝트 운용 계획서 v5

> 스피노자 『에티카』의 논리적 구조를, 관람객이 걸어다니며 체험하는 **웹 3D 건축 공간**으로 번역하는 개인 포트폴리오 프로젝트.
> 1인 작업 · 협업 파트너: Claude · **목표 = 상한 2026.08.31 (방학 끝 = 진짜 벽, 협상 불가)** · 1차 용도: **공모전·전시·공개**
>
> 🔄 **개정 v5 (2026.07.28, 계획 점검 결정 세션):**
> ① **목표일 8/19 → 8/31 이동 확정**(현도 2026.07.26 결정, v4 §8에 미반영이던 것을 여기서 정본화). 이로써 **목표 = 상한**이 되고 v4가 갖고 있던 버퍼 12일이 예산으로 전환됐다.
> ② **조형 예산 14일 신설**(7/28–8/10). 현도: *"넣고 싶은 디테일 아이디어가 엄청나게 많이 생기고 있다. 구조물 조형에 시간을 더 줘야 한다."* 구 P1(7/13–7/30)의 연장이 아니라 **재배정**이다.
> ③ **순서 = 조형 먼저**(현도 판단). 대안이던 '조명 먼저'(조형을 확정 조명 아래에서 지어 재판정을 없앰)는 기각. → **P2 하드 게이트 날짜가 7/31 → 8/11로 이동.**
> ④ **아이디어 티켓제 폐기**(현도). v4가 티켓을 만든 상황(예산 없이 계획 외 5건 유입)이 해소됐다 — 조형 예산 14일이 명시적으로 크다. 대체 장치는 §8 운용규칙 1.
> ⑤ **P3 완주 준비 6일 → 5일 축소**(현도 판단 — 조형 세션마다 로컬 순회로 확인하고 있고 문제가 거의 없다). ⚠**단서는 §8 운용규칙 2에.**
> ⑥ **테라스(1p12~15) = 별도 결정 세션 폐지.** 조형 14일 안의 디테일 항목 하나로 내린다(현도: *"테라스가 중요한 게 아니다"*). v4 운용규칙 2(테라스 = P1 마지막 + 결정 세션 선행)는 이로써 소멸.
> ⑦ **버퍼 2일 = 리스크 수용 기록**(§8 말미). 실측(막다른 길 5일당 1회)과 충돌하는 값이며, 현도가 근거를 듣고 선택했다.
> 이전 판 v4(2026.07.13) 대체. **v4 파일은 repo에서 삭제**(연대기는 노션에 보존).

---

## 1. 완결의 정의

- 에티카 **1부(정리 1~15)** 를 웹에서 탐험 가능한 3D 공간으로 구현. **깊이 우선** — 끝까지 구현된 1부가 얕은 5부보다 압도적으로 강하다.
- **1차 용도가 공모전·전시·공개이므로:** 심사자·관람객은 링크를 열고 2분 안에 판단한다. "정리 15개가 전부 있다"보다 **"관람객의 첫 2분이 강하다"** 가 우선한다. 배포 링크·워크스루 영상·작품 스테이트먼트는 부록이 아니라 **작품의 절반**이다.
- 조형의 완성도가 첫 2분을 결정한다(친구 "Roblox" 소견 = 조명·렌더링 부재 진단). **조형 확장 + 조명이 작품의 몸통**, 완주·발표물이 그것을 전달한다.
- **16~36(사물의 생산)은 범위 외 확정.** 방학 후 별도 사이클.
- **데스크톱(마우스+키보드) 전용.** 모바일 대응·사운드는 범위 외.

## 2. 기술 스택 (확정 — 합의 없이 변경 금지)

| 계층 | 선택 | 비고 |
|---|---|---|
| 런타임 | React Three Fiber (Three.js) + Vite | 1인칭·인스턴싱·거리별 텍스트·절차 생성 |
| 기하 불리언 | three-bvh-csg | 겹침·개구부는 정공법 CSG |
| 배포 | Vercel (`ethica-architectonics.vercel.app`) | push 자동 재배포 |
| 모델링 | Blender (강등) | 조건부 승격 시에만 |

- **drei 기각:** 정리 텍스트 품질 불만 시 `<Text>`(SDF)만 재론.
- 반복·절차적 구조는 코드로 생성. 손모델링 없음.

## 3. 정본 3분할 (진실의 원천)

| 무엇 | 어디 | 규칙 |
|---|---|---|
| **코드** | GitHub `https://github.com/Hyeondo1209/ethica-architectonics` | 읽기 = `git clone --depth 5` **만**. raw.githubusercontent.com 금지(Fastly 옛 캐시 반환, 검증됨) |
| **설계 상태** | repo 루트 `DESIGN.md` | 단일 정본. 세션마다 **고쳐 쓴다**. §3(공간 모델)은 의도·불변식·핵심 노브만 — 수치 정본은 코드 주석 |
| **연대기** | 노션 'Ethica Architectonics 프로젝트 일지' DB | "언제 무엇을 했나"만 |

⚠ repo는 로컬보다 뒤질 수 있다(push 수동). 시작 의례의 대조 규칙(§6)이 간극을 잡는다.

## 4. 스코프 대장 (v5 재편)

**확정**
- 자유 탐험(FirstPersonControls). 공개(1p11) 순간만 부분 연출 가능성 열어둠.
- 조기 배포(Vercel) — 연결 완료.
- DoD 5항 = P3 출구 조건(§8).
- **조형 14일 예산**(§8) — 티켓제를 대체한다.
- **테라스(1p12~15)** — 조형 14일 안의 디테일 항목. 별도 결정 세션 없음.

**강등 (조건부 승격)**
- 블렌더 스피노자 조각상 → 대체: 입구 명판/타이틀 카드.
- **별자리 최소형(Tab 오버레이+의존선) → 강등 확정** — DoD-4는 비석 확장으로 충족. 승격 조건: P3 조기 통과 + 여유.

**범위 외**
- 16~36 / 모바일 / 사운드.

**열림 (결정 대기)**
- **1p8 계단 권역의 새 그림**(★76 반려로 원점) — 현도. Claude 선제 제안 금지.
- 조형 디테일 목록 — 현도 머릿속에 있고 세션마다 지시로 나온다.
- 리브 실제 팔레트·fog↔공개·보행속도 — P2·P3.

## 5. 역할 분담

- **김현도:** 철학적 해석의 최종 판단 · 미적 결정 · 파라미터 미세조정 · GitHub Desktop Commit→Push(유일한 수동 단계, 코드 세션 당일 push 원칙).
- **Claude:** 구조화 · 구현 · 비판적 검토 · 문서화(DESIGN.md 갱신, 노션 일지 기록).
- **스케치 규칙:** 손 스케치는 반드시 Claude 리드백 → 김현도 명시적 확인 → 구현.
- ⚠**설계 제안 금지선:** 현도의 그림이 서기 전에 Claude가 조형 아이디어를 선제 제안하지 않는다(반려율 실측). Claude가 하는 일은 **재기·규명·구현·검증**이다.

## 6. 세션 의례 (v3·v4 계승 — 변경 없음)

**시작**
1. `git clone --depth 5` → **DESIGN.md 제일 먼저** + 주요 소스 정독, `git log` 확인. 클론 실패 시 멈추고 물을 것(기억으로 리드백 금지).
2. 노션 최신 일지(**날짜 기준**) 확인 → 클론 HEAD가 일지 '① 오늘 한 일'을 포함하는지 대조. ⚠️UNPUSHED거나 불포함이면 로컬>repo로 간주, 사용자 확인. ⚠️UNPUSHED 태그 청소(push 확인 시 제거).
3. 현재 그림 **리드백** → 첫 줄에 '이 세션 = <유형> — 권장 <등급>' 선언 → 작업.

**종료**
1. `DESIGN.md` 갱신(고쳐 쓰기).
2. 노션 일지: ①오늘 한 일 ②현재 상태 ③다음 할 일(각 항목 [A/B급·무게] 태그) ④열린 결정. 제목 `YYYY.MM.DD … — <산출물 한 줄>`, 코드 push 전이면 ⚠️UNPUSHED.
3. push 대상 파일 목록 명시 → 사용자 Commit→Push.

**세션 유형:** 결정 세션 vs 코드 세션. 한 세션 = 산출물 하나. 코드 전달은 바뀐 모듈만, 항상 present_files.

## 7. 프로젝트 지시문

v4 §7 블록 계승. ⚠**v5에서 사용자가 프로젝트 설정에서 교체해야 할 것 둘:**
- 파일명 참조 `Ethica_Architectonics_운용계획_v4.md` → **`_v5.md`**
- 마감 문구 `목표 8/19 · 상한 8/31` → **`목표 = 상한 8/31`**

나머지 규칙 전부 유효.

## 8. 일정 — 4 Phase (v5 · 목표 = 상한 8/31)

> **기준일 2026.07.28. 남은 캘린더 35일.** 고정비 = P2 조명 7 + P3 완주 5 + P4 발표물 7 = 19일.
> **추정 근거(실측):** 큰 신규 조형 1건 = 캘린더 2일. 막다른 길(전량 반려)은 **5일당 1회**(7/13~7/27 15일에 3회 — ㊵·1p2 계열 2회 + ★76).

| Phase | 기간 | 산출물 | 출구 조건 |
|---|---|---|---|
| **P1′ 조형 확장** | **7/28 – 8/10 (14일)** | ① **현도 디테일 목록 소화** — 세션마다 지시로 나온다. 이 예산이 그 상한이다 ② **1p8 계단 권역**(★76 반려로 원점 — 현도 새 그림 대기) ③ **테라스(1p12~15) 구조물** — 별도 결정 세션 없이 디테일 항목으로 ④ **`render_views` 전망 갈래 정본 교체**(구 램프를 그리는 버그 — P2 진입 전 필수) ⑤ **`check_waypoints` 보행 절 신설**(§8 운용규칙 2) | **형태 동결** — 이후 조형은 노브 튜닝만 |
| **P2 조명·미학** | **8/11 – 8/17 (7일)** | ⑥ **전역 조명 개편**(AO·톤 매핑·조명 레이어 — Manifold Garden 참조) ⑦ 그 빛 아래 **미학 재판정 일괄**: P3 실루엣·정점 미광·리브 실제 팔레트·등불 유래감·1p2 절벽면·개구 그림자(DESIGN.md §7 대기 6건) | 미학 판정 완료 |
| **P3 완주 준비** | **8/18 – 8/22 (5일)** | ⑧ **비석 11기 확장**(1p5~15) + 담체 디자인 분화 `2.5일` ⑨ **공개(1p11) 연출** + fog·보행속도 `1.5일` ⑩ **완주 최종 확인**(`FREE_WALK=false`, 방→테라스) `0.5일` ⑪ `SPAWN='room'` 복귀 · `DEV_TELEPORT=false` `0.5일` | **DoD 5항 전부 ✅** |
| **P4 발표물** | **8/23 – 8/29 (7일)** | ⑫ 워크스루 영상(2~3분) ⑬ 작품 스테이트먼트(A급 후보 — Claude 초안, 김현도 확정) ⑭ README·제출 정리 | 공개 |
| **버퍼** | **8/30 – 8/31 (2일)** | — | — |

**🚩 DoD (P3 출구 조건 — 전부 '예'여야 통과):**
1. 15개 정리 모두 '집'이 있다 *(✅ — 단 1p6은 결정만 완료·코드 미구현, `UNASSIGNED=[6]` 존치)*
2. 방→테라스를 충돌 켠 상태로 걸어서 완주할 수 있다
3. 공개(1p11) 순간이 의도대로 작동한다 (fog·시선 포함)
4. 여정 중 정리 텍스트를 읽을 수 있다 (비석 11기로 충족 — 별자리 불요)
5. 배포 링크로 제3자가 체험할 수 있다 *(✅)*

**운용 규칙 (v5 — 티켓제 폐기에 따른 재편):**

1. **예산 자체가 장치다(티켓제 대체).** **조형 14일이 끝나면 8/11에 P2로 넘어간다 — 아이디어가 남아 있어도 넘어간다.** 남은 것은 ⓐ P2 조명 아래에서 계속하거나(하드 게이트 조항이 허용) ⓑ 방학 후 사이클로. 중간 점검은 두지 않는다(현도 결정 2026.07.28).
2. ⚠**P3 5일은 "이미 걸어봤다"를 전제로 선 값이다 — 그 전제를 코드가 지킨다.** 현 로컬 순회는 `FirstPersonControls.jsx`의 `FREE_WALK = true` 상태이며, 이 모드는 `probe()`를 호출하지 않아 **벽을 통과하고 바닥이 없어도 낙하하지 않는다**(L128·L143). 즉 **형태는 검증되고 있으나 보행 가능성은 아직 시험된 적이 없다.** 전례 셋(★60 환형 허공 0.85 / ★62 링 슬롯 0.40 / ★63 우물로 인한 횡단 무효)이 전부 "보기엔 멀쩡한데 밟을 수 없는" 종류였다. → **P1′ 안에서 `check_waypoints`에 보행 절을 신설**해 ⓐ `walkable` 태그 누락(현재 59곳 — ★75 디딤판 68장·갈림판·아치 바닥·★62 칼라·★63 발코니 미확인) ⓑ 접합부 단차 > `STEP_UP` 0.8 을 코드로 잰다. **현도 시간 소모 0.** 계단 자체는 R 0.19~0.21로 안전(여유 4배) — 위험은 이음매와 태그뿐이다.
3. **P2 = 하드 게이트.** 8/10에 조형이 덜 끝났어도 조명은 **8/11 시작** — 남은 조형은 조명 아래에서 계속(오히려 판정이 정확해짐).
4. ⚠**버퍼 2일 = 리스크 수용(기록).** 막다른 길 실측이 5일당 1회이므로 **1회만 나도 마감선을 먹는다.** 또 P4의 워크스루 영상·스테이트먼트는 **한 번도 만들어본 적이 없어** 7일이 낙관적일 여지가 있다. 현도가 이 근거를 듣고 조형 14일을 선택했다(2026.07.28). **미통과 조짐이 보이면 P4를 줄여서라도 DoD 먼저.** 상한 8/31 = 방학 끝 = 협상 불가.

## 9. 현재 위치 (2026.07.28, v5 개편 시점)

- **repo HEAD** `c6e6d8a`(07/27 22:34) — ★75 넓은 상승 계단 + 중앙 아치까지 포함. 07/27 세션(★76)은 **구현 후 현도 반려·전량 원복**이라 repo 무변경.
- **검증** 6종 스위트 **858항 green**(corridor 518 · waypoints 169 · rooms 65 · radial 57 · lamps 24 · lens 25) + 빌드 green.
- **완성:** 15개 정리 전부 집 확보 — 방(정의·공리) → 방사 4방 → 드럼 통로(1p5 다섯 문) → 리브 #0 나선(1p6 오벨리스크·결정만) → 프리즈 방(1p7) → #+2 갈아타기 → 무릎길 → 갈림 → 1p8 넓은 상승 계단(막다름) / 아치 → 회랑(1p9) → 등불(1p10) → 문(1p11) → 테라스(1p12~15) → 정점 렌즈.
- **남은 핵심:** 조형 디테일(현도 목록) · 1p8 새 그림 · 테라스 구조물 · 조명 개편 · 비석 11기 · 공개 연출 · 발표물.
- **알려진 미해결:** `render_views.mjs` 전망 갈래가 ★75가 폐기한 구 램프를 그림(셀프 렌더가 이 권역에서 거짓말) · `_probe_view` 자동 검사 미승격 · 하강로 꼬리 무지지 스팬 95(현도 "남는 시간에") · 회랑·전실 벽 두께 0 면.

## 10. 모델 라우팅 (v3·v4 계승 — 등급은 역할로 정의)

| 등급 | 정의 | 세션 유형 |
|---|---|---|
| **A(최상급)** | 그 시점의 최상급 모델(현 Fable) | **신규 기하 최초 구현**(CSG·곡면·절차 생성 — 테라스 구조물·1p8 새 조형이 여기) · 구조 대수술 · 로컬 재현 어려운 버그 · (여유 시) 스테이트먼트 초안 |
| **B(표준)** | 그 외(Opus 등) | 결정·매핑 세션 · 기존 패턴 복제·확장(비석 11기 등) · 파라미터 튜닝 · 조형 부분 수정 · 문서·일지 · 발표물 정리 |

운용 규칙(불변): 종료 태깅 → 시작 선언·불일치 경고 → A급 세션 수칙(크게-적게·산출물 하나·결정 사전 봉합·첫 메시지에 몰아주기). 사용자 명시 지시가 라우팅보다 우선.

**v5 적용 노트:** 조형 14일의 디테일 수정은 대부분 B급(기존 기하 수정·노브). A급 후보 = 테라스 구조물 신규 기하 · 1p8 계단 권역 새 조형 · P2 조명 개편(전역 셰이더/레이어 체계면 A급).
