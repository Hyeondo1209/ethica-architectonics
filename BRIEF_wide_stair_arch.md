# ★75 넓은 상승 계단 + 중앙 아치 — 구현 스펙

> 현도 스케치(2026.07.25 `갈림길_스케치.jpg`) · **리드백 확인 완료** · 티켓 **미사용**(현도 결정)
> 이 문서는 **다음 세션의 첫 메시지**로 투입한다(㊳ Fable 브리프 전례 — repo 안에 둔다).
> ⚠**착수 전 반드시**: 클론 → DESIGN.md §6 → `git log`로 ★70~★74 반영 여부 확인.

---

## 1. 확정된 그림 (현도 확인 완료)

1. **전망 램프(폭 2 · 35칸 · 50.9°)를 관 폭을 채우는 넓은 계단으로 교체.**
2. **그 계단 중앙에 아치 입구**를 뚫는다. 위치 = **계단 맨 아래**(현도 확정).
3. **하강 계단이 그 아치로 들어가 넓은 계단 밑을 터널로 지나간다**(현도 확정).
   → 하강 z가 **+1.75(옆) → 0(중앙)** 으로 이동.
4. **상승 계단 시작 = 갈림판 −x변**(`X_LAND_LO` 184.51). 판 자체가 현도가 말한 "갈림판 정도의 여유"다.
5. **반원 판은 그대로**(위치·크기·두께 무변경 — 현도 확정).
6. 스케치 중앙의 세로 띠 = **★71 빛 기둥**(이미 구현됨 — 신규 아님).

**논증적 의미(현도 판정 사항):** 갈림이 좌우(z 분리)에서 **위·아래**로 바뀐다. 크고 당당한 계단이 막다름(1p8)이고, 갈 길은 그 바닥에 뚫린 문이다.

---

## 2. 실측 (착수 시 재확인할 것 — 노브가 바뀌면 따라 변한다)

| 항목 | 값 |
|---|---|
| 계단 수평 주행 (판 −x변 184.51 → 반원 판 174.62) | **9.89** |
| 올라야 할 높이 (256.03 → 268.18) | **12.15** |
| 현행 경사 | **50.85°** |
| 관이 허용하는 계단 폭 (경로 전 구간) | **11.2 ~ 11.5** (현행 2 = 17%) |
| 하강을 z=0으로 옮겼을 때 계단 밑 머리 여유 | x183.3에서 **3.0** → x178.5에서 **14.6** |
| 판 −x쪽 관 z반폭 (판 높이) | x184.5 **5.64** · x182 **5.13** · x180 **4.32** · x178.5 **3.27** |

### ⚠ 경사가 이 안의 최대 쟁점이다
평평한 여유 L을 계단 앞에 넣으면 남은 주행으로 같은 높이를 올라야 한다:

| L | 남은 주행 | 경사 |
|---|---|---|
| 0 | 9.89 | 50.85° |
| 2 | 7.89 | 57.0° |
| 4 | 5.89 | 64.1° |
| 6 | 3.89 | 72.2° |

→ **L=0(판 −x변에서 바로 시작)만 성립한다.** 그래서 1-4가 그렇게 확정됐다.

**단 51°는 그 자체로 급하다.** 현도는 무릎길 38~40°를 "급하다"고 판정했고 하강 49°도 톱니로 남아 있다.
**폭이 넓어지면 급함이 더 드러난다** — 좁은 램프는 사다리처럼 넘어가지만 폭 11짜리 기념비 계단이 51°면 못 걷는다.

**반원 판을 −x로 물려 주행을 버는 안**(`LK_DISC_DX`): 실측 상한은 **−4**다(그 이상 물리면 판 자리 관 z반폭이 5.5 미만이 되어 판이 관 밖으로 나간다).
DX −4 → 주행 11.89 → L=0에서 **45.6°**. 그래도 완만하진 않다.
→ **경사를 낮추려면 반원 판 높이(`LK_DISC_DY` 또는 `LOOKOUT_MAX_SLOPE`)를 건드려야 한다. 이건 1p8의 도달 높이라 현도 판단 사항.**

---

## 3. 구현 순서 (이 순서가 아니면 재작업이 난다)

### ① 하강 z 이동 (`JCT_DN_Z` 1.75 → 0)
- ★인수분해가 잘 되어 있다: 채널 슬랩·볼벽 둘·아치 컷(`ARCH_Z*`)·방 입구가 **전부 파생**이라 상수 한 줄로 따라온다.
- ⚠⚠**그런데 전 스위트가 green으로 통과한다 — 이건 안전의 증거가 아니다.**
  ★74에서 신설한 봉인 검사는 볼벽 상단을 **판 기준 높이**로 잠글 뿐이고, 시선 자체는 안 잰다.
  누출 실측은 z=1.75 기하에서 한 것이라 **z=0에서는 전부 다시 재야 한다.**
- → **착수 시 첫 작업 = 누출 차분 검사 재구축.** 모델 규율(2026.07.25 실패 3회에서 얻음):
  - **포함 판정**을 쓴다(교차 판정은 살 안쪽 경계를 놓친다)
  - **표본 간격 < 살 두께(0.22)** — 0.1 이하
  - 절댓값이 아니라 **차분**(값을 바꿨을 때 늘어나는가)만 본다
  - 모델 두 벌이 같은 답을 내도 **정확성의 증거가 아니다**(둘 다 다른 이유로 틀릴 수 있다 — 실증)

### ② 넓은 상승 계단
- 신설 위치 = `junctionGeometry.js`(정션 권역 정본).
- 폭은 **★72 판 윤곽과 같은 수법**: z 반폭을 상수가 아니라 **관의 함수**로. 융착 `JCT_PLATE_FUSE` 전례.
- 어휘는 **§2-D**: 매스 + 깎인 밑면(허공 판 금지). 밑면이 곧 터널 천장이 되므로 ★65 `buildKneeBody`(각기둥 ∩ 관 안쪽)와 같은 구성이 자연스럽다.
- 규격은 **블롱델부터 재유도**(★66 전례). `STEP_RISE` 0.35는 폐기된 기하의 유산 의심 대상.

### ③ 중앙 아치 입구
- 계단 맨 아래, z=0 중심. 폭은 하강 채널(`2×PASS_HW` = 4.6)에 맞추는 것이 자연스럽다.
- 계단 매스에서 **감산**(CSG). ⚠자르개는 **딱 필요한 범위만**(★64-5: 넘치면 관벽이 유령으로 남는다).
- 계단 밑면(터널 천장)과 하강 보행선 사이 머리 여유를 검사로 잠글 것(시작부 3.0이 최소).

### ④ 검증
- U절 확장: 계단 폭이 관을 따르는가 · 아치가 뚫렸는가(**광선**으로) · 터널 머리 여유 · 하강이 계단 밑을 실제로 지나는가 · 봉인 재측정 결과 잠금.
- 웨이포인트 `lookout`·`junction` 좌표 재유도(계단이 바뀌면 따라온다).
- `render_views`: 이미 통로 권역·하강·전망·매듭·빛 기둥·레이크 볼벽까지 굽는다(2026.07.25 보강). `DIAG_PASS=1` 진단 모드 있음.

---

## 4. 손대는 파일 (예상)

`src/constants.js` · `src/junctionGeometry.js` · `src/Dome.jsx`(Lookout 전면 재작성 · RibJunction) ·
`src/check_corridor.mjs` · `src/render_views.mjs` · `src/waypoints.js` · `DESIGN.md`

---

## 5. 열린 것 (현도 판단 대기)

- **경사 51°를 그대로 둘 것인가** — 폭 11짜리 계단에서 51°는 걷기 어렵다. 낮추려면 반원 판 높이를 건드려야 하고 그건 1p8의 도달 높이다.
- **아치 폭** — 4.6(채널 폭)인가 더 넓은가.
- **정션 판과 계단 첫 단의 이음** — 판(★70~★72)이 그대로 남고 계단이 그 −x변에서 시작한다.
- ⚠**P2 조명 하드 게이트 7/31.** 이 작업에 들어가면 ㉠(규격 재유도·하강/전망 몸 잇기)과 ㉣(회랑 벽 두께)은 이번 방학 범위 밖이 된다.
