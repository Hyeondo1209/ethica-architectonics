# Ethica Architectonics — 설계 정본 (DESIGN.md)

> **이 문서는 "지금 설계가 *무엇인가*"의 단일 정본이다.**
> 운용 규칙·일정·의례 전문 = repo 루트 `Ethica_Architectonics_운용계획_v3.md`(2026.07.02 확정). 노션 일지 = 연대기.
> 멘탈 모델: **코드 = GitHub · 설계상태 = 이 문서 · 연대기 = 노션 일지.**
>
> ## 세션 의례 요약 (전문은 운용계획 §6)
> - **시작:** ① `git clone --depth 5`(§8 주소; raw 금지 — Fastly 옛 캐시, 검증됨) → **이 문서 제일 먼저** + 주요 소스 정독 + `git log`. ② **대조: 클론 HEAD가 노션 최신 일지의 '① 오늘 한 일'을 포함하는가?** 제목에 ⚠️UNPUSHED가 있거나 불포함이면 **로컬 > repo로 간주**, 사용자에게 확인. (실증: 2026.07.02 PropStele 간극.) ③ 현재 그림 **리드백** 후 작업.
> - **끝:** ① 이 문서 **고쳐 쓰기**(덧붙이지 않음; §3은 의도·불변식·핵심 노브만 — 수치 정본은 코드 주석). ② 노션 일지(제목에 산출물 한 줄; 코드 세션인데 push 전이면 ⚠️UNPUSHED). ③ push 대상 명시 → 사용자 Commit→Push(당일 원칙).

---

## 0. 완결의 정의 / 방법론 / 역할
- **완결 = 에티카 1부(정리 1~15)의 웹 3D 건축 번역.** 깊이 우선. **마감 2026.07.31(방학 끝) · 1차 용도 공모전·전시·공개** → "첫 2분이 강한가"가 "15개가 다 있는가"보다 우선(운용계획 v3 §1). 16~36은 **이번 방학 범위 외**(확정).
- **방법론:** 마르샬 게루의 발생적-구조적 독해. 신을 *전제*하지 않고, 단일 속성 실체에서 신 개념을 *구성*해 나가는 과정으로 1부를 파악. (들뢰즈 비교, 내들러 보조.)
- **역할:** 철학적 해석의 최종 판단·미적 결정 = **사용자(김현도)**. Claude = 구조화·구현·비판적 검토. **사용자의 독해를 멋대로 바꾸지 않는다.** 손 스케치는 리드백→명시 확인→구현.
- **스택(확정):** React Three Fiber(three.js)+Vite, three-bvh-csg, 배포 Vercel(Phase 1). **합의 없이 변경 금지.** drei 기각(텍스트 품질 문제 시 `<Text>`만 재론). 블렌더 강등(입구 명판으로 대체, 조건부 승격). 반복·절차 구조는 코드로 생성.

## 1. 못 박은 원칙 (LOCKED — 절대 잊지 말 것)
- ★★ **단일 속성 실체 = 항상 얇고 우아하게 휜(S자 / 탄젠트 곡선) "경선 리브" 그 자체.** 두꺼운 별도 기둥이 아니다. 리브 1개 = 단일 속성 실체 1개. 여럿(72)이 무한 반복을 암시. **→ 전환 완료**(별도 반투명 기둥 `ClimbShaft`·두꺼운 `Pillars` 모두 삭제됨).
- ★★ **탐험 = 그 경선 리브 *중 하나*(φ=0, `RIB_PHI`) 안의 나선을 오르는 것.** 별도 종류의 구조물이 아니라 한 리브의 *역할*일 뿐. → **탐험 리브를 위치·굵기·형태로 차별화하면 안 된다**(전부 하나의 실체). 들어갈 굵기는 *공간 전체 스케일업*으로 흡수 → **완료: `SCALE=3.5`.**
- ★★ **리브는 불투명(opaque)이다.** 나선을 오르는 동안엔 그 리브 *내부*만 보인다 → **"무한히 많은 리브" 공개는 등반 *중*이 아니라 꼭대기 도착 시**에 일어난다. (등반 중 점진 공개 / 리브 반투명 아이디어는 폐기.) 시야 차단 장치는 **두 개로 분리**: ① 리브 불투명(등반 격리) ② 통로 외피(접근 중 먼 리브 가림). *(검토 발견 2026.07.02: 공개의 물리적 지점은 참·직선계단이 열린 돔 공간에 있어 사실상 **리브 출구(참 진출) 순간** — "꼭대기 도착"의 해석·정리 배치는 다음 세션에서 확정, §5 미결 포크.)*
- **동선:** **지상 정의·공리 방(큰 돔의 열린 중심=원점)** → 방 안 나선 올라 빛우물 디스크 → **박스 연결부** → **거대 원기둥 통로**(평면 다리 → 원형 플랫폼(정리 자리) → 계단) → 가장자리 탐험 리브 → 리브 안 나선 → 참·직선계단 → 테라스(도착). **중앙에서 출발 → 수평 바깥 → 가장자리에서 위.**
- **돔 = 내재성('외부 없음').** 정의·공리 방 = **큰 리브 돔 중앙의 지상 돔**(시작점; 옛 '지하' 폐기). 정리 1~4 = **거대 원기둥 통로 안의 길**(평면 다리·플랫폼·계단).
- **외피 = 거대 원기둥(벽 + 닫힌 빗면 천장) + 슬랜더 박스 연결부.** 원기둥이 정리 1~4 구간을 감싼다 = **스포 방지** + **공간감**. **정면(+x) 창**으로 탐험 리브 #0+이웃 ±2만 보이고 ±3+는 벽이 가림. ★ 테이퍼 통로는 공간감 소실로 폐기 — *원기둥은 항상 굵게, 연결부만 가늘게.*
- **길 = 평면 다리(방→플랫폼) + 원형 플랫폼(기둥이 지면까지 받침) + 계단(플랫폼→리브).** 플랫폼 = **정리가 놓이는 자리**(현재 1p1 비석, 추후 사슬 여부는 배치 골격에서).

## 2. 용어집 (GLOSSARY — 의도 정렬용)
| 영역 | 용어 | 코드 이름 | 철학적 의미 | 형태/상태 |
|---|---|---|---|---|
| 외피 | **돔**(열린 돔) | `DomeRibs` 집합 | 내재성. 단일 속성 실체들의 무한 반복 → 하나의 실체(신)로의 이행 | 지표에서 위로 좁아지며 열림 |
| 실체 | **경선 리브** | `DomeRibs` | **단일 속성 실체** (리브 1 = 실체 1; 72 = 무한 반복) | 얇고 휜 **불투명** 리브 72개 |
| 탐험 | **탐험 경선**(φ=0) + **나선** | φ=0 리브 + `RibStair` | 안에서 올라 체험하는 단일 속성 실체 / 나선=상승 | ✅ 리브 *안* 나선 + 중앙 폴 |
| 도착 | **테라스**(+참·직선계단) | `Terrace`,`LandingPad`,`StraightFlight` | 등반의 도착·조망점 | 중앙 부분 링 |
| 정점 | **정점 / 광원** | `Apex` | 신·무한의 빛(열린 천장) | y=H 광원+구 |
| 토대 | **지상 정의·공리 방** | `DefAxiomRoom` | 주어진 것들(정의·공리). 시작점 | 지상 납작 돔(원점)+내벽 나선+오큘러스+착지 링+원뿔대 빛우물(CSG) |
| 통로 | **거대 원기둥 + 박스 연결부** | `Corridor` | 정리 1~4를 감싸 스포 방지 + 공간감 | ✅ 원기둥+빗면천장+창(±2)+박스 연결부(CSG) |
| 길 | **다리 + 플랫폼 + 계단** | `Corridor` 내부 + `Stairs` | 정리 1~4가 놓이는 경로 | ✅ |
| 정리 텍스트 | **정리 비석** | `PropStele`+`makeSteleTexture` | 정리 본문의 담체(슬랩은 늘 보이고 글자만 거리별 페이드) | ✅ 시제품(1p1, 플랫폼) — 위치·크기·형태 미확정 |
| 도구 | **데이터 그래프 뷰** | `GraphScaffold` / `ethica1.js` | (건축 아님) 의존 구조 점검 | `G`키 토글 |
| 폐기 | ~~반투명/두꺼운 기둥·공중통로·작은참·깔때기·테이퍼·링 마커·지하 b버전~~ | — | ✗ 낡은 표현 | 코드에서 제거됨 |

## 3. 공간 모델 — 의도·불변식·핵심 노브 (★수치의 정본 = 코드 주석. 여기엔 '왜'만 남긴다)
- **스케일 불변식:** `SCALE=3.5`는 **건축 치수 전용**. 사람 치수(EYE·계단 보폭 등)는 고정 → 사람만 상대적으로 작아져 리브 안에 들어감. *(검토 지적: `WALK_SPEED=3.5×SCALE`는 이 원칙과 상충 — 속도를 키우면 체감 스케일이 상쇄됨. 릴리즈 전 결정: 사람 속도 고정+달리기 키 vs 현행. §7)*
- **돔/리브:** 곡선 `rOf(u)`=tanh 프로파일, **무릎(KNEE=0.25) 아래는 거의 수직(원통)**. 리브 72개(빽빽한 밀도 선호)·불투명. 탐험 리브는 φ=0이되 형태는 전 리브 동일(LOCKED).
- **리브 나선(`RibStair`):** 리브 중심선을 따라 무릎 살짝 아래(`U_LAND=0.22`)까지. 칸 수 = 높이÷보폭 자동. ⚠️ **현재 u=0(y=0, 지면)에서 시작** — 통로 도착 높이 `RIB_Y`와 불일치 + 리브 튜브에 **출입 개구부 없음** → Phase 1-③에서 CSG 문 + 시작 높이 `RIB_Y` 기준으로 정합(§6).
- **방(`DefAxiomRoom`):** 불변식 — ① `ROOM_CEIL_Y=49`(돔 apex=통로 접합, **변경 금지**) ② 내벽 나선 도착 반지름 `RIN` < 오큘러스 반지름(≈17.45)이어야 관통 ③ 빛우물=**원뿔대**(위 좁음=리브 스포 차단, 바닥=착지 링과 봉합) ④ 겹침(원뿔대−박스−돔)은 **CSG 정공법**(`HOLLOW_SUBTRACTION`), 면별 근사 금지. **내벽 나선 = 공중 낱장 디딤판, 높이 기준 배치**(칸마다 RISE 상승 → 가파른 안쪽 자동 촘촘). **형태 = 원형(매끈) 확정**, 옥타곤은 `T`키 A/B 비교로 보존. ★핵심 상충 원리: 나선이 클수록(ROUT·TURNS↑) 경로가 길어 경사↓·칸 성김 — 계단다움은 **오직 경로길이 축소**로만 얻는다. 현 균형점 = ROUT 45·2바퀴·bias 1.0(고른 상승으로도 헤드룸 통과).
- **통로(`Corridor`):** 불변식 — ① **`COR_CYL_X0`가 원기둥 크기를 방 위치와 분리하는 핵심 노브**(원기둥 반지름 `COR_R`은 방 위치 무관 고정) ② 창 반각 `WIN_HALF=asin(42/COR_R)` = **물리 z폭 42 보존 공식**(±2 보임/±3 가림, `COR_R`이 바뀌어도 유지) ③ 박스 연결부는 원기둥에 살짝 물리고(`BOX_X1=COR_CYL_X0+4`), 박스−원뿔대 겹침은 CSG ④ 방 돔↔원기둥 겹침은 `domeClipY`(벽 밑을 돔 표면까지 클립). 빗면 천장(낮은 방쪽→높은 리브쪽)이 공간감의 핵심.
- **길:** 다리(평면, 상승 0=접근 시야) → 플랫폼(정리 자리, 받침 기둥) → 계단(자동 칸수)으로 `RIB_Y`까지 상승.
- **정리 비석(`PropStele`):** 본문은 `ethica1.js`에서 읽음(**텍스트=데이터** 원칙). 슬랩·받침 늘 보임, **글자만** smoothstep 거리 페이드(near/far 노브). 앞면 −x(접근 방향).
- **조작/임시:** `FREE_WALK=true`(편집용 — Phase 1-④에서 끔). 시작 = 방 바닥(원점), +x 바라봄. `G` 그래프, `T` 방 계단 비교.

## 4. 요소 인벤토리 (코드명 · 상태)
| 코드 | 상태 |
|---|---|
| `rOf` / `ribCenter` / `DomeRibs` / `Ground` / `Apex` | ✅ |
| `RibStair` | ✅ 단, 시작 y=0·개구부 없음(§3·§6) |
| `LandingPad` / `StraightFlight` / `Terrace` | ✅ |
| `DefAxiomRoom` | ✅ 원뿔대 CSG(`wellCut`)+원형 내벽 나선(옥타곤 T키 보존) |
| `Corridor` | ✅ 원기둥+빗면천장+창±2+박스 연결부 CSG(`boxWallCut`/`boxCeilCut`). 구조 확정 |
| `Stairs` / 다리·플랫폼·기둥 | ✅ |
| **`PropStele` + `makeSteleTexture`** | ✅ **시제품(1p1, 플랫폼 중앙)** — 위치("저곳 아님")·크기·형태 미확정 |
| `FirstPersonControls` | ✅ `FREE_WALK` 임시 켜짐 |
| `GraphScaffold` / `ethica1.js` | ✅ 1~8 데이터(17노드·19간선), 9~15 미입력 |

## 5. 정리 배치·표현 현황 (PROPOSITION MAPPING — 2026.07.02 진단 + 검토)
> **"어디서 만나는가(집)"와 "무엇을 봐서 이해하는가(표현 매체)"는 별개.** 공백 정리 다수는 새 지오메트리 없이 **이미 씬에 있는 현상에 시선을 걸어** 표현 가능.
> ⚠️ 현 여정은 물리적으로 **1p8까지만** 설명됨. 1p9~15는 집·표현 공백.

| 정리 | 집(여정 위치) | 표현 매체 — 현 상태 | 판정 |
|---|---|---|---|
| 정의·공리 | 지상 방 | '주어진 것들'(텍스트, 형식 미정) | 배치 O · 텍스트 |
| 1p1 실체>변용 | 통로 플랫폼 | **비석 시제품(PropStele)** — 위치·크기 미확정 | 배치 O · **표현 착수** |
| 1p2 다른 속성=공통점 없음 | 통로 창 뷰 | **창 밖 5리브 무간섭(안 닿음)** | ✅ device 확정(미구현) |
| 1p3 공통 없음→인과 없음 | 통로 창 | 1p2와 같은 시각의 2독해(검토 제안) | 제안 있음 |
| 1p4 속성/변용으로 구분 | 통로 | 텍스트 tier(검토 권고) | 제안 있음 |
| 1p5 동일 속성 둘 불가 | 미정: 나선 vs **통로 끝**(검토 권고) | '리브 1=실체 1' + 눈앞의 리브 | 미결 포크 |
| 1p6 타 실체서 산출 불가 | 리브 문/나선 | 무간섭의 회고 인용(deps=[1p5,1p2,1p3]) | 제안 있음 |
| 1p7 본성에 존재 | 나선(진입) | **자기 건축 순간 필요** — device 후보 (a)진입 사건 (b)내려다보기 | 미결 |
| 1p8 필연적 무한 | 나선 등반 | 끝없는 높이(suo genere 무한 — `ethica1.js` note) | 배치 O · 스타일 |
| 1p9 실재성↑→속성↑ | 검토 제안: **나선 상부(출구 직전 문턱)** | 텍스트(이행) | 제안 있음 |
| 1p10 속성은 자기 통해 파악 | 〃 | 〃 | 제안 있음 |
| 1p11 신 필연 존재 | 검토 제안: **출구·참 진출**(공개의 물리적 지점) | **무한 리브 공개** | ✅ device 확정·핵심 |
| 1p12 분할되면 파악 불가 | 검토 제안: 테라스 | 텍스트 | 제안 있음 |
| 1p13 절대 무한 불가분 | 〃 | 이음새 없는 하나의 돔(시선) | 제안 있음 |
| 1p14 신 외 실체 없음 | 〃 | 올려다보기: 한 정점 수렴(⚠️fog 상호작용 §7) | 제안 있음 |
| 1p15 만물은 신 안에 | 〃 | 공간 그 자체('외부 없음' LOCKED) | 제안 있음 |

**미결 포크(다음 세션=배치 골격에서 확정):** (a) **9~15 3분할안**(문턱 9·10 / 출구 11 / 테라스 12~15 — 검토 제안, 전부 기존 지오메트리) 채택 여부 — 대안 ①테라스 일괄 ②별도 구간. (b) **1p5 위치**(통로 끝 권고). (c) **1p7 device**(a/b/보류). (d) 1p4 텍스트 tier 승인. (e) **"인식의 질서" 독해**(스포 방지 벽=부적합한 인식의 조건 — 채택 시 스테이트먼트 서사 강화).

## 6. 다음 작업 (운용계획 v3 §8 Phase 1: 7/3~7/10)
**최근 완료(2026.07.02, 두 세션):** ① 오전 — §5 진단 방법론(집/표현 분리+3분류). ② 오후 — **Claude Fable 5 전면 검토**(계획·코드·설계 3축; 스포 방지 감사 통과) + **중간 점검 → 운용계획 v3 확정**(마감 7/31·용도 공개·전시·4 Phase·DoD·대조 규칙·스코프 7건 비준). PropStele 간극 발견·본 문서 교정.

1. **★ 배치 골격 확정** (결정 세션 — 다음 세션): §5 미결 포크 (a)~(e) 결정 → §5를 확정판으로 고쳐 씀.
2. **파일 분할 + Vercel 배포**: App.jsx(872줄) 모듈 분리(상수/컨트롤/방/통로/리브/비석), 이후 전달은 바뀐 모듈만. 배포 15분.
3. **리브 입구 기하**: 튜브에 CSG 문 + 나선 시작을 `RIB_Y` 기준으로(§3).
4. **걷기·충돌 패스**: `FREE_WALK=false`, 방→테라스 완주 가능.

**백로그(검토 지적 — 해당 파일 만질 때 처리):** 죽은 상수 6개 삭제(`WELL_DOOR_HALF`·`WELL_DOOR_TOP`·`ROOM_CLIMB_H`·`DEF_RING_R`·`AXIOM_RING_R`·`MARKER_R`) · CSG 공유 수치 상수화(`ROOM_CEIL_Y-3` 등 중복 제거) · `FirstPersonControls`의 walkable 매 프레임 traverse → 1회 수집 · `COR_CLIMB` 옛 코멘트(≈9.3°→실제 18.9°) · 나선 16바퀴 어지러움 플레이테스트.

## 7. 열린 / 미룬 결정
- **★ 배치 골격 5건(§5 미결 포크)** — 다음 세션.
- **리브 차이 채널**(색/재질/곡률) — 판단 기준: "속성 = 같은 본질의 다른 관점" 독해 채택 여부(동일 형태 유지의 근거가 됨).
- **fog ↔ 공개 연출**: fog far(525) = 테라스→정점 거리 — 공개 순간에 정점·리브 수렴이 안개에 잠김. '무한=사라짐' vs 1p14 시선 — 로컬 확인 후 튜닝.
- **보행속도**: 사람 고정(2.5~4)+달리기 키 vs 현행 ×SCALE — 로컬 확인 후.
- **PropStele 위치·크기·형태** — 담체=비석·글자만 페이드는 확정, 나머지 사용자 튜닝.
- 9~15 의존 데이터(`ethica1.js`) 입력 — 배치 골격 후.
- 16~36 확장 — 방학 후 별도 사이클(범위 외 확정).

## 8. 인프라 좌표
- **코드:** GitHub 공개 `https://github.com/Hyeondo1209/ethica-architectonics` — 읽기 = `git clone --depth 5` 만(raw 금지). 주요 파일: `src/App.jsx`, `src/ethica1.js`, `src/GraphScaffold.jsx`.
- **정본 문서:** `DESIGN.md`(설계 상태) + `Ethica_Architectonics_운용계획_v3.md`(운용 규칙·일정) — 둘 다 repo 루트.
- **연대기:** 노션 'Ethica Architectonics 프로젝트 일지' DB.
- **로컬:** VS Code · `C:\Users\김현도\ethica-architectonics` · `npm run dev` → localhost:5173. GitHub Desktop **Commit→Push** = 유일한 수동 단계.
- **이번 세션(2026.07.02 오후) push 대상: `DESIGN.md`(교체) + `Ethica_Architectonics_운용계획_v3.md`(신규, repo 루트).** 코드 변경 없음(`src/App.jsx`는 HEAD `6eae660`으로 이미 최신).
