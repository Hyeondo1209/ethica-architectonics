# Ethica Architectonics — 설계 정본 (DESIGN.md)

> **이 문서는 "지금 설계가 *무엇인가*"의 단일 정본이다.**
> 노션 일지는 "언제 *무엇을 했나*"의 연대기. 이 둘은 분리한다 — 섞으면 연결성이 깨진다.
>
> ## 세션 의례 (반드시 지킬 것)
> - **시작:** ① **이 문서를 제일 먼저 읽는다.** ② 현재 그림(원칙·용어·공간모델·인벤토리·열린결정)을 사용자에게 **리드백**한 뒤 작업을 시작한다(어긋남을 작업 중간이 아니라 *시작에* 잡기 위해). ③ 코드는 `curl`로 읽는다(아래 주소). 노션 최신 일지는 최근 연대기 참고용.
> - **끝:** ① **이 문서를 갱신한다**(덧붙이지 말고 *현재 상태로 고쳐 쓴다*). ② 노션 일지에 그 세션의 연대기를 *풍족히* 남긴다(무엇을·왜·산출물).
> - **코드 읽기는 항상 `curl`.** `web_fetch`는 GitHub raw에서 옛 캐시를 반환한다(검증됨).
> - **repo는 보통 뒤처져 있다.** 사용자는 내 App.jsx를 로컬에 반영해 돌리지만 push는 가끔 한다 → repo를 curl하면 옛 코드일 수 있음. 최신은 항상 직전에 전달한 App.jsx 기준. 사용자가 push할 때 App.jsx + DESIGN.md를 함께 올린다.
> - 멘탈 모델: **코드 = GitHub · 설계상태 = 이 문서 · 연대기 = 노션 일지.**

---

## 0. 완결의 정의 / 방법론 / 역할
- **완결 = 에티카 1부(정리 1~15)의 웹 3D 건축 번역.** 5부 전체 아님. **깊이 우선**(끝까지 구현된 1부 > 얕은 5부). 16~36(사물의 생산)은 욕심 목표.
- **방법론:** 마르샬 게루의 발생적-구조적 독해. 신을 *전제*하지 않고, 단일 속성 실체에서 신 개념을 *구성*해 나가는 과정으로 1부를 파악. (들뢰즈 비교, 내들러 보조.)
- **역할:** 철학적 해석의 최종 판단·미적 결정 = **사용자(김현도)**. Claude = 구조화·구현·비판적 검토. **사용자의 독해를 멋대로 바꾸지 않는다.**
- **스택(확정):** React Three Fiber(three.js). 주연 에셋만 Blender→glTF. 배포 Vercel. **합의 없이 스택 변경 금지.** 반복·절차적 구조는 코드로 생성(손모델링 X).

## 1. 못 박은 원칙 (LOCKED — 절대 잊지 말 것)
- ★★ **단일 속성 실체 = 항상 얇고 우아하게 휜(S자 / 탄젠트 곡선) "경선 리브" 그 자체.** 두꺼운 별도 기둥이 아니다. 리브 1개 = 단일 속성 실체 1개. 여럿(72)이 무한 반복을 암시. **→ 전환 완료**(별도 반투명 기둥 `ClimbShaft`·두꺼운 `Pillars` 모두 삭제됨).
- ★★ **탐험 = 그 경선 리브 *중 하나*(φ=0, `RIB_PHI`) 안의 나선을 오르는 것.** 별도 종류의 구조물이 아니라 한 리브의 *역할*일 뿐. → **탐험 리브를 위치·굵기·형태로 차별화하면 안 된다**(전부 하나의 실체). 들어갈 굵기는 *공간 전체 스케일업*으로 흡수 → **완료: `SCALE=3.5`.**
- ★★ **리브는 불투명(opaque)이다.** 나선을 오르는 동안엔 그 리브 *내부*만 보인다 → **"무한히 많은 리브" 공개는 등반 *중*이 아니라 꼭대기(테라스) 도착 시**에 일어난다. (등반 중 점진 공개 / 리브 반투명 아이디어는 폐기.) → 시야 차단 장치는 **두 개로 분리**: ① 리브 불투명(등반 격리) ② 통로 외피(접근 중 먼 리브 가림).
- **동선:** 지하 정의·공리 방(중앙 근처) → **막힌 깔때기 통로 안** 평면 다리 → 원형 플랫폼(정리 자리) → 완만한 계단 다리 → 가장자리 탐험 리브 → 리브 안 나선 → 참·직선계단 → 테라스(도착). **중앙(지하) → 수평 바깥 → 가장자리에서 위.**
- **돔 = 내재성('외부 없음').** 정의·공리 = **지하**. 정리 1~4 = **막힌 깔때기 통로 안의 길**(평면 다리·플랫폼·계단).
- **막힌 깔때기 통로(벽+천장)** 가 정리 1~4 구간을 감싼다 = **스포 방지.** 통로가 먼 리브들을 가려서 꼭대기 공개가 미리 새지 않게 한다. 정면(+x)만 액자처럼 열려 탐험 리브 #0와 이웃 ±2만 보인다.
- **길 = 평면 다리(방→플랫폼) + 원형 플랫폼(기둥이 지면까지 받침) + 완만한 계단(플랫폼→리브).** 플랫폼 = **정리가 놓이는 자리**(우선 1p1 하나, 추후 1p1~1p4 사슬: 다리-플랫폼-다리…). 평면 다리는 접근하며 앞 시야가 트이고, 계단은 마지막 리브 접합 구간에만 둔다.

## 2. 용어집 (GLOSSARY — 의도 정렬용)
| 영역 | 용어 | 코드 이름 | 철학적 의미 | 형태/상태 |
|---|---|---|---|---|
| 외피 | **돔**(열린 돔/뒤집힌 깔때기) | `DomeRibs` 집합 | 내재성. 단일 속성 실체들의 무한 반복 → 하나의 실체(신)로의 이행 | 지표 r60 → 천장 r35로 좁아지며 열림, 높이 200 (×SCALE) |
| 실체 | **경선 리브** | `DomeRibs` | **단일 속성 실체** (리브 1 = 실체 1; 72 = 무한 반복) | 얇고 휜 **불투명** 리브 72개 |
| 탐험 | **탐험 경선**(φ=0 리브) + **나선** | `DomeRibs`의 φ=0 리브 + `RibStair` | 안에서 올라 체험하는 단일 속성 실체 / 나선=상승 | ✅ 전환 완료(리브 *안* 나선 + 중앙 폴) |
| 도착 | **테라스**(+참·직선계단) | `Terrace`,`LandingPad`,`StraightFlight` | 등반의 도착·조망점 | 중앙 부분 링, 나선→참→직선계단으로 연결 |
| 정점 | **정점 / 광원** | `Apex` | 신·무한의 빛(열린 천장) | y=H 광원+구 |
| 토대 | **지하 정의·공리 방** | `DefAxiomRoom` | 주어진 것들. 정의 1d1~8(안쪽 링)·공리 1a1~7(바깥 링) | b버전: footprint 유지·바닥 아래로 팜·천장 낮음. 마커 15 |
| 통로 | **막힌 깔때기 통로**(외피) | `Corridor` | 정리 1~4를 감싸 먼 리브 가림(스포 방지) | 벽+천장, 정면만 열림. ✅ ⚠️ **다음 세션 재디자인 논의**(공간감) |
| 길 | **평면 다리 + 원형 플랫폼 + 계단** | `Corridor` 내부 + `Stairs` | 정리 1~4가 놓이는 경로(방→리브) | ✅ NEW. 평면 다리 → 플랫폼(정리 자리) → 계단 |
| 정리 자리 | **원형 플랫폼**(기둥 받침) | `Corridor` 내부 mesh | 정리(1p1…)가 놓이는 발판 | ✅ NEW. 얇은 원판 + 받침 기둥 |
| 바닥 | **지반** | `Ground` | 지표면 | 평면 |
| 도구 | **데이터 그래프 뷰** | `GraphScaffold` / `ethica1.js` | (건축 아님) 1~8 의존 구조 점검용 와이어프레임 | `G`키 토글 |
| 폐기 | ~~반투명 기둥/두꺼운 기둥/공중통로/작은참~~ | `ClimbShaft`/`Pillars`/`Walkway`/`SmallLandingPad` | ✗ 모두 잘못/낡은 표현 | **코드에서 이미 제거됨** |

## 3. 공간 모델 + 현재 파라미터 (현 코드 = 최신 App.jsx 기준; repo는 아직 옛날)
- **스케일:** `SCALE=3.5`. 모든 건축 치수에 곱함. 사람 치수는 고정.
- **돔/리브:** `R_BASE=60×S=210`(지표) → `R_TOP=35×S=122.5`(천장 개구부), `H=200×S=700`. 곡선 `rOf(u)`: `KNEE=0.25`(u공간, 고정)에서 `tanh`(`WIDTH=0.02`)로 급히 좁아짐 — **무릎 아래(u<~0.22)는 거의 수직(원통, r≈210)**. `MERIDIANS=72`, 리브 굵기 `SHELL_RIB_R=1.1×S=3.85`(**불투명**, DoubleSide). `RIB_PHI=0`(탐험 리브 각도; 단 모든 리브 동일). **선호 밀도 = 빽빽한 72개.**
- **나선(`RibStair`, φ=0 리브 *안*):** `U_LAND=0.22` → `Y_LAND=H×0.22=154`(나선 끝 높이, 무릎 살짝 아래). `STEP_RISE=0.32`(고정) → `STAIR_STEPS≈481`(자동=높이÷보폭), `STEPS_PER_TURN=30` → 약 16바퀴. `STAIR_R=SHELL_RIB_R×0.55≈2.12`(리브 내부에 들도록 굵기 비례). 디딤판 `TREAD_DEPTH=1.5`/`TREAD_WIDTH=1.0`/`TREAD_THICK=0.20`, 중앙 폴 `POLE_R=0.35`.
- **테라스:** `TERRACE_Y=H×KNEE=175`, `TERRACE_RIN=27×S=94.5`~`TERRACE_ROUT=33×S=115.5`, `TERRACE_ARC=2.4`(+x). 참 `LandingPad`(`PAD_SIZE=6`) + 직선계단 `StraightFlight`(`FLIGHT_LEN` 자동, `FLIGHT_WIDTH=3.5`)로 나선 top(154)→테라스(175) 연결.
- **정점:** `Apex` y=H=700, 광원+구.
- **지하 방(b버전):** `ROOM_CX=R_BASE×0.2=42`(중앙↔리브 비율; 0=중앙,1=리브), `ROOM_FLOOR_R=22×S=77`(footprint), `ROOM_CEIL_Y=14×S=49`(천장 apex=통로 접합 높이), `ROOM_HEIGHT=36×S=126`(아래로 팜), `ROOM_FLOOR_Y=49−126=−77`. 정의 링 `DEF_RING_R=7×S=24.5`(마커 8)·공리 링 `AXIOM_RING_R=12×S=42`(마커 7), `MARKER_R=0.9×S=3.15`. 정의=안쪽/따뜻한색, 공리=바깥/차가운색. 반투명 천장 돔(opacity 0.45 — **씬에서 유일한 반투명 요소**).
- **막힌 깔때기 통로(`Corridor`):** `COR_Y0=ROOM_CEIL_Y=49`(길 바닥 높이=방 천장 apex), `COR_X0=ROOM_CX=42`(방쪽 접합), `COR_X1=R_BASE=210`(리브쪽). 좁은 바닥폭 `COR_FLOOR_HW=4.5`. 벽/천장 높이 `COR_WALL_H=60` → `COR_WALL_TOP=109`(천장 절대 높이; 날개벽은 지면 0~109 한 장으로 아래 시야 차단). 날개(개구부) 시작 `COR_FLARE_X=COR_X0+0.2×(길이)=75.6`, 개구부 반폭 `COR_MOUTH_HW=42`(이웃 ±2 보임/±3 가림). 두께 `COR_THICK=0.6`. 모드: **`flare`(2단, 확정)** / `fan`(부채 — **죽은 코드, 마무리 시 제거**). `C`키 토글(임시).
- **길(`Corridor` 안, 두 모드 공통):** **평면 다리**(`COR_X0`→`PLAT_X−PLAT_R`, y=`COR_Y0`=49, 상승 0 → 접근 시야 트임) + **원형 플랫폼**(`PLAT_X=126`=통로 중간, `PLAT_R=11`, y=`PLAT_Y=COR_Y0`=49, **정리 자리**) + **받침 기둥**(`PILLAR_R=4`, 플랫폼 밑→지면 0) + **완만한 계단**(`PLAT_X+PLAT_R`→`COR_X1`, y=49→`RIB_Y`). 계단(`Stairs`): 칸 높이=두께 `COR_RISE=0.43`, 다리2 총 상승 `COR_CLIMB=5`, 칸수 `COR_STEPS=round(5/0.43)=12`(자동), 경사≈**3.9°**. `RIB_Y=COR_Y0+COR_CLIMB=54`(리브 접합 높이). **계단은 다리2(플랫폼→리브)에만**, 다리1은 평면.
- **사람 스케일(고정):** 눈높이 `EYE=1.6` 등. **스케일업 시 이 값들은 고정**, 건축 치수만 키운다. 걷기 = `userData{walkable:true}` 표면에 레이캐스트로 높이 맞춤.
- **조작/임시:** `FREE_WALK=true`(벽·낙하 충돌 끔 — 편집용 임시; 끄려면 false). `G`키 돔↔데이터 그래프 뷰. `C`키 통로 모양(임시). **시작 위치 = 통로 안쪽**(`COR_X0+3`, `COR_Y0+1.6`), +x(리브) 바라봄.

## 4. 요소 인벤토리 (코드명 · 상태)
| 코드 | 상태 |
|---|---|
| `rOf` / `ribCenter` | ✅ 곡선 헬퍼 |
| `DomeRibs` | ✅ 단일 속성 실체(불투명 리브 72) |
| `Ground` / `Apex` | ✅ |
| `RibStair` | ✅ φ=0 리브 *안* 나선 + 중앙 폴 |
| `LandingPad` / `StraightFlight` / `Terrace` | ✅ 나선→참→직선계단→테라스 |
| `DefAxiomRoom` | ✅ b버전(깊게 팜); 입구·하강·라벨 미완 |
| `Corridor` | ✅ 막힌 깔때기(flare/fan). ⚠️ **다음 세션 재디자인** / fan·C토글 죽은 코드 제거 예정 |
| `Stairs` | ✅ **NEW** 다리2 계단(속찬 얇은 칸; tread/riser 트릭 아님) |
| (Corridor 내부) 평면 다리·원형 플랫폼·받침 기둥 | ✅ **NEW** |
| `FirstPersonControls` | ✅ `FREE_WALK` 임시 켜짐 |
| `GraphScaffold` / `ethica1.js` | ✅ 점검 도구; 1~8 데이터, 9~15 미입력 |
| ~~`ClimbShaft`·`Pillars`·`Walkway`·`SmallLandingPad`~~ | ❌ **이미 제거됨**(코드에 없음) |

## 5. 다음 세션의 핵심 작업 (NEXT SESSION)
**막힌 깔때기 통로(`Corridor` 외피)의 대안 디자인 논의.**
- 사용자가 **새 스케치를 준비**함. 이유: 현재 막힌 깔때기 외피가 **공간감이 부족**해 보임(닫힌 느낌).
- 길 구조(평면 다리 → 플랫폼 → 계단)는 어느정도 자리잡음 — **외피(벽·천장)를 어떻게 바꿀지**가 논점. 단 외피의 *기능*(먼 리브 가림 = 스포 방지)을 유지하면서 공간감을 살리는 방향으로.
- 부수 정리: `fan`(부채) 모드·`C`키 토글 **죽은 코드 제거**. **1p1 텍스트**를 플랫폼 위에 얹기(현재 텍스트 메커니즘 없음).

## 6. 열린 / 미룬 결정
- **막힌 깔때기 외피 재디자인(공간감) — 다음 세션 핵심.**
- 통로 끝 ↔ 리브 안 나선 입구 **높이 맞춤**(현재 길은 `RIB_Y=54`에서 끝나지만 나선과 정밀 연결 안 됨).
- 길 계단 경사/두께 미세조정(`COR_CLIMB`/`COR_RISE`) — 현재 3.9°/0.43, 사용자 "어느정도 OK".
- 플랫폼 1개 → **1p1~1p4 사슬**(다리-플랫폼-다리…) 확장.
- 정리 텍스트(1p/1d/1a) 표시 방식 + **메커니즘**(현재 코드에 텍스트 표시 없음).
- 방 입구·하강 동선(방 천장 apex 구멍 + 코너 직선계단 → 통로 안쪽). 시작 위치를 방 안으로.
- 9~15 의존 데이터 입력(형태 먼저, 데이터 뒤 — 보류).
- drei 도입 여부. `FREE_WALK` 임시(편집 끝나면 false).
- **테라스 "무한 리브" 공개 연출 실제 구현**(불투명 리브라 *여기*가 공개 지점).
- 16~36 확장 여부(욕심 목표).

## 7. 인프라 좌표
- **코드(단일 원천):** GitHub 공개 `https://github.com/Hyeondo1209/ethica-architectonics`
  - raw(curl로 읽기): `https://raw.githubusercontent.com/Hyeondo1209/ethica-architectonics/main/src/App.jsx` (그 외 `src/GraphScaffold.jsx`, `src/ethica1.js`)
- **이 정본:** `https://raw.githubusercontent.com/Hyeondo1209/ethica-architectonics/main/DESIGN.md` (저장소 루트)
- **연대기:** 노션 'Ethica Architectonics 프로젝트 일지' DB.
- **로컬:** VS Code · `C:\Users\김현도\ethica-architectonics` · `npm run dev` → http://localhost:5173. 코드/문서 변경 후 GitHub Desktop **Commit→Push**가 유일한 수동 단계. (이번 세션: `src/App.jsx` + `DESIGN.md` 둘 다 갱신 → 함께 push.)
