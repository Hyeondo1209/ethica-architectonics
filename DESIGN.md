# Ethica Architectonics — 설계 정본 (DESIGN.md)

> **이 문서는 "지금 설계가 *무엇인가*"의 단일 정본이다.**
> 노션 일지는 "언제 *무엇을 했나*"의 연대기. 이 둘은 분리한다 — 섞으면 연결성이 깨진다.
>
> ## 세션 의례 (반드시 지킬 것)
> - **시작:** ① **이 문서를 제일 먼저 읽는다.** ② 현재 그림(원칙·용어·공간모델·인벤토리·열린결정)을 사용자에게 **리드백**한 뒤 작업을 시작한다(어긋남을 작업 중간이 아니라 *시작에* 잡기 위해). ③ 코드는 `git clone --depth 5`로 읽는다(§8 주소; raw는 옛 캐시라 금지). 노션 최신 일지는 최근 연대기 참고용.
> - **끝:** ① **이 문서를 갱신한다**(덧붙이지 말고 *현재 상태로 고쳐 쓴다*). ② 노션 일지에 그 세션의 연대기를 *풍족히* 남긴다(무엇을·왜·산출물).
> - **코드 읽기는 항상 `git clone`.** raw.githubusercontent.com은 `web_fetch`·`curl`(`?cb=` 캐시버스터 포함) **모두** Fastly 옛 캐시를 조용히 반환한다(검증됨). `api.github.com`은 공유 egress IP에서 rate-limit → **clone이 지반.**
> - **repo는 보통 뒤처져 있다.** 사용자는 내 App.jsx를 로컬에 반영해 돌리지만 push는 가끔 한다 → clone해도 push 전이면 옛 코드일 수 있음. 최신은 항상 직전에 전달한 App.jsx 기준. 사용자가 push할 때 App.jsx + DESIGN.md를 함께 올린다.
> - 멘탈 모델: **코드 = GitHub · 설계상태 = 이 문서 · 연대기 = 노션 일지.**

---

## 0. 완결의 정의 / 방법론 / 역할
- **완결 = 에티카 1부(정리 1~15)의 웹 3D 건축 번역.** 5부 전체 아님. **깊이 우선**(끝까지 구현된 1부 > 얕은 5부). 16~36(사물의 생산)은 욕심 목표.
- **방법론:** 마르샬 게루의 발생적-구조적 독해. 신을 *전제*하지 않고, 단일 속성 실체에서 신 개념을 *구성*해 나가는 과정으로 1부를 파악. (들뢰즈 비교, 내들러 보조.)
- **역할:** 철학적 해석의 최종 판단·미적 결정 = **사용자(김현도)**. Claude = 구조화·구현·비판적 검토. **사용자의 독해를 멋대로 바꾸지 않는다.**
- **스택(확정):** React Three Fiber(three.js). 주연 에셋만 Blender→glTF. 배포 Vercel. **합의 없이 스택 변경 금지.** 반복·절차적 구조는 코드로 생성(손모델링 X).

## 1. 못 박은 원칙 (LOCKED — 절대 잊지 말 것)
- ★★ **단일 속성 실체 = 항상 얇고 우아하게 휜(S자 / 탄젠트 곡선) "경선 리브" 그 자체.** 두꺼운 별도 기둥이 아니다. 리브 1개 = 단일 속성 실체 1개. 여럿(72)이 무한 반복을 암시. **→ 전환 완료**(별도 반투명 기둥 `ClimbShaft`·두꺼운 `Pillars` 모두 삭제됨).
- ★★ **탐험 = 그 경선 리브 *중 하나*(φ=0, `RIB_PHI`) 안의 나선을 오르는 것.** 별도 종류의 구조물이 아니라 한 리브의 *역할*일 뿐. → **탐험 리브를 위치·굵기·형태로 차별화하면 안 된다**(전부 하나의 실체). 들어갈 굵기는 *공간 전체 스케일업*으로 흡수 → **완료: `SCALE=3.5`.**
- ★★ **리브는 불투명(opaque)이다.** 나선을 오르는 동안엔 그 리브 *내부*만 보인다 → **"무한히 많은 리브" 공개는 등반 *중*이 아니라 꼭대기(테라스) 도착 시**에 일어난다. (등반 중 점진 공개 / 리브 반투명 아이디어는 폐기.) → 시야 차단 장치는 **두 개로 분리**: ① 리브 불투명(등반 격리) ② 통로 외피(접근 중 먼 리브 가림).
- **동선:** **지상 정의·공리 방(큰 돔의 열린 중심=원점)** → 방 안 나선 올라 빛우물 디스크 → **박스 연결부** → **거대 원기둥 통로**(평면 다리 → 원형 플랫폼(정리 자리) → 계단) → 가장자리 탐험 리브 → 리브 안 나선 → 참·직선계단 → 테라스(도착). **중앙에서 출발 → 수평 바깥 → 가장자리에서 위.**
- **돔 = 내재성('외부 없음').** 정의·공리 방 = **큰 리브 돔 중앙의 지상 돔**(시작점; 옛 '지하' 폐기). 정리 1~4 = **거대 원기둥 통로 안의 길**(평면 다리·플랫폼·계단).
- **외피 = 거대 원기둥(벽 + 닫힌 빗면 천장) + 슬랜더 박스 연결부.** 원기둥이 정리 1~4 구간을 감싼다 = **스포 방지**(불투명 벽이 먼 리브 밭 가림) + **공간감**(천장 빗면 70→150 솟음, airtight). **정면(+x) ±30° 창**으로 탐험 리브 #0+이웃 ±2만 보이고 ±3+는 벽이 가림. **방(원점)↔원기둥은 슬랜더 박스 연결부로 이음**(박스 폭=빛우물 실린더 폭). ★ 테이퍼 통로는 공간감 소실로 폐기 — *원기둥은 항상 굵게, 연결부만 가늘게.*
- **길 = 평면 다리(방→플랫폼) + 원형 플랫폼(기둥이 지면까지 받침) + 계단(플랫폼→리브).** 플랫폼 = **정리가 놓이는 자리**(우선 1p1 하나, 추후 1p1~1p4 사슬: 다리-플랫폼-다리…). 평면 다리는 접근하며 앞 시야가 트이고, 계단은 마지막 리브 접합 구간에만 둔다.

## 2. 용어집 (GLOSSARY — 의도 정렬용)
| 영역 | 용어 | 코드 이름 | 철학적 의미 | 형태/상태 |
|---|---|---|---|---|
| 외피 | **돔**(열린 돔/뒤집힌 깔때기) | `DomeRibs` 집합 | 내재성. 단일 속성 실체들의 무한 반복 → 하나의 실체(신)로의 이행 | 지표 r60 → 천장 r35로 좁아지며 열림, 높이 200 (×SCALE) |
| 실체 | **경선 리브** | `DomeRibs` | **단일 속성 실체** (리브 1 = 실체 1; 72 = 무한 반복) | 얇고 휜 **불투명** 리브 72개 |
| 탐험 | **탐험 경선**(φ=0 리브) + **나선** | `DomeRibs`의 φ=0 리브 + `RibStair` | 안에서 올라 체험하는 단일 속성 실체 / 나선=상승 | ✅ 전환 완료(리브 *안* 나선 + 중앙 폴) |
| 도착 | **테라스**(+참·직선계단) | `Terrace`,`LandingPad`,`StraightFlight` | 등반의 도착·조망점 | 중앙 부분 링, 나선→참→직선계단으로 연결 |
| 정점 | **정점 / 광원** | `Apex` | 신·무한의 빛(열린 천장) | y=H 광원+구 |
| 토대 | **지상 정의·공리 방**(큰 돔 중앙) | `DefAxiomRoom` | 주어진 것들(정의·공리). 시작점 | **지상 납작 돔**(r91, apex49, 원점). 내벽 나선 → 빛우물 디스크. 옛 지하 b버전·링 마커 폐기 |
| 통로 | **거대 원기둥**(벽+빗면천장) **+ 박스 연결부** | `Corridor` | 정리 1~4를 감싸 먼 리브 가림(스포) + 공간감 | ✅ 원기둥(r84, x42~210) + 빗면천장(70→150) + 창(±2) + 방↔원기둥 박스 연결부 |
| 길 | **평면 다리 + 원형 플랫폼 + 계단** | `Corridor` 내부 + `Stairs` | 정리 1~4가 놓이는 경로(방→리브) | ✅ 평면 다리 → 플랫폼(정리 자리) → 계단 |
| 정리 자리 | **원형 플랫폼**(기둥 받침) | `Corridor` 내부 mesh | 정리(1p1…)가 놓이는 발판 | ✅ 얇은 원판 + 받침 기둥 |
| 바닥 | **지반** | `Ground` | 지표면 | 평면 |
| 도구 | **데이터 그래프 뷰** | `GraphScaffold` / `ethica1.js` | (건축 아님) 1~8 의존 구조 점검용 와이어프레임 | `G`키 토글 |
| 폐기 | ~~반투명 기둥/두꺼운 기둥/공중통로/작은참/깔때기 외피~~ | `ClimbShaft`/`Pillars`/`Walkway`/`SmallLandingPad`/(flare·fan) | ✗ 모두 잘못/낡은 표현 | **코드에서 이미 제거됨** |

## 3. 공간 모델 + 현재 파라미터 (현 코드 = 직전 전달 App.jsx; repo는 뒤처질 수 있음)
- **스케일:** `SCALE=3.5`. 모든 건축 치수에 곱함. 사람 치수는 고정.
- **돔/리브:** `R_BASE=60×S=210`(지표) → `R_TOP=35×S=122.5`(천장 개구부), `H=200×S=700`. 곡선 `rOf(u)`: `KNEE=0.25`(u공간, 고정)에서 `tanh`(`WIDTH=0.02`)로 급히 좁아짐 — **무릎 아래(u<~0.22)는 거의 수직(원통, r≈210)**. `MERIDIANS=72`, 리브 굵기 `SHELL_RIB_R=1.1×S=3.85`(**불투명**, DoubleSide). `RIB_PHI=0`(탐험 리브 각도; 단 모든 리브 동일). **선호 밀도 = 빽빽한 72개.**
- **나선(`RibStair`, φ=0 리브 *안*):** `U_LAND=0.22` → `Y_LAND=H×0.22=154`(나선 끝 높이, 무릎 살짝 아래). `STEP_RISE=0.32`(고정) → `STAIR_STEPS≈481`(자동=높이÷보폭), `STEPS_PER_TURN=30` → 약 16바퀴. `STAIR_R=SHELL_RIB_R×0.55≈2.12`(리브 내부에 들도록 굵기 비례). 디딤판 `TREAD_DEPTH=1.5`/`TREAD_WIDTH=1.0`/`TREAD_THICK=0.20`, 중앙 폴 `POLE_R=0.35`.
- **테라스:** `TERRACE_Y=H×KNEE=175`, `TERRACE_RIN=27×S=94.5`~`TERRACE_ROUT=33×S=115.5`, `TERRACE_ARC=2.4`(+x). 참 `LandingPad`(`PAD_SIZE=6`) + 직선계단 `StraightFlight`(`FLIGHT_LEN` 자동, `FLIGHT_WIDTH=3.5`)로 나선 top(154)→테라스(175) 연결.
- **정점:** `Apex` y=H=700, 광원+구.
- **지상 정의·공리 방(`DefAxiomRoom`):** `ROOM_CX=0`(**큰 리브 돔의 열린 중심=원점**). `ROOM_R=26×S=91`(바닥=가장 넓은 반지름), `ROOM_CEIL_Y=14×S=49`(돔 apex=통로 접합, **변경 금지**), `ROOM_FLOOR_Y=0`(지상), `ROOM_HEIGHT=49`. **불투명 납작 돔**(sphereGeometry, apex에 오큘러스)+바닥 원판+내부 pointLight. **오큘러스 `ROOM_OCULUS=0.193`**(반지름≈17.45 @ y≈48.09 — 나선이 이 열린 구멍으로 관통). **착지 디스크=링**(`ROOM_DISC_HOLE=6`~`ROOM_LAND_R=18`, 윗면 y=49.3, 슬롯 `SLOT_START=67°`·`SLOT_LEN=301°`→약 52° 틈=나선 진입, +x). **빛우물=원뿔대(frustum)**: 바닥 반지름 18(y≈46)→꼭대기 `ROOM_WELL_RT=2.5`(`ROOM_CYL_TOP=110`, 위 열림), **CSG 정밀 절단**(`three-bvh-csg`, `HOLLOW_SUBTRACTION`: 원뿔대−박스−돔타원체→`wellCut`), +x 하단 출입문 트임·꼭대기 down-light(좁은 시야로 빛만, 벽이 리브 가림=스포). **내벽 나선계단(`ROOM_STAIR_*`) — 공중에 뜬 낱장 디딤판이 중심으로 감기며 상승**(바깥 `ROUT`=바닥 → 중심 `RIN`=꼭대기 디스크): `SIDES=8`, `TURNS=2`, `ROUT=45`→`RIN=14`(오큘러스 17.45보다 작아 관통), `RISE=0.35`(칸 균일높이), `WIDTH=2.4`, `TREAD=4.0`(디딤판 깊이), `SLAB=0.35`(두께), `BIAS=1.0`(조각별 등높이), `PHASE=−TURNS·2π`(꼭대기 +x). **높이 기준 배치**: `nStep=round(CLIMB/RISE)`, 칸마다 정확히 RISE 상승→경사 급한 데는 자동 촘촘. **★계단 형태 = 원형(매끈) 확정**(기본); **옥타곤(8각 각짐)은 `T`키 A/B 비교로 보존** — 둘 다 같은 파라미터·낱장 디딤판, **경로 형태만** 다름(`helixInsts` vs 옥타곤 `insts`). ⚠️ 옛 helix(`TOPR`/`FLARE`/`INSET`/`STEPS`·속찬 디딤판·스트링거)·실린더 빛우물·b버전(지하·링 마커·반투명 천장)은 **전부 폐기**(코드에 없음).
- **거대 원기둥 + 박스 연결부(`Corridor`):** `COR_Y0=49`(길 바닥=방 apex), `COR_X0=ROOM_CX=0`(전체 안쪽 끝=방), `COR_X1=210`(리브). **★ `COR_CYL_X0=42`**(거대 원기둥 안쪽 시작=박스 연결부 바깥 끝; **원기둥 크기를 방 위치와 분리하는 핵심 노브**, ↓=원기둥 큼). **거대 원기둥:** 중심 `COR_CX=(42+210)/2=126`, 반지름 `COR_R=(210−42)/2=84`(**고정 — 방 위치 무관**), 둘레 `COR_WALL_SEG=96`, 바닥 원이 지면0에 앉음. **빗면 천장:** `CEIL_LO=70`→`CEIL_HI=150`, `ceilY(x)=70+slope·(x−42)`(slope=`COR_CYL_X0` 기준). 벽=바닥0/돔표면→`ceilY` 둘레 띠, 천장=중심→림 삼각부채(닫힘). **정면 창(±2):** `WIN_HALF=asin(42/COR_R)=π/6`(물리 z폭 42 유지→±2 보임/±3 가림; COR_R 바뀌어도 보존), `WIN_SILL_Y=0`~`WIN_TOP_Y=150` 풀하이트. **박스 연결부(방↔원기둥):** `BOX_X0=COR_X0=0`(방 중심)~`BOX_X1=COR_CYL_X0+4=46`(원기둥 안으로 물림), 반폭 `BOX_HW=6`(=빛우물 실린더), 천장 `BOX_TOP=70`, 원기둥 −x 문 `DOOR_HALF=asin(6/84)`. 판 두께 `COR_THICK=0.6`. **방 돔(r91)↔원기둥(−x부 42~91) 겹침**은 `domeClipY`로 벽 밑을 돔 표면까지 잘라 처리. (옛 테이퍼 통로·깔때기 flare/fan·`C`토글 **폐기**.)
- **길(`Corridor` 안):** **평면 다리**(`COR_X0`→`PLAT_X−PLAT_R`, y=49, 상승 0 → 접근 시야 트임) + **원형 플랫폼**(`PLAT_X=(0+210)/2=105`, `PLAT_R=11`, y=49, **정리 자리**) + **받침 기둥**(`PILLAR_R=4`, 플랫폼 밑→지면 0) + **계단**(`PLAT_X+PLAT_R`→`COR_X1`, y=49→`RIB_Y`). 길 반폭 `COR_FLOOR_HW=2.5`(폭 5). 계단(`Stairs`): 칸 높이 `COR_RISE=0.43`(고정), 총 상승 `COR_CLIMB=25`, 칸수 `COR_STEPS=round(25/0.43)=58`(자동), run 73 → 경사≈**18.9°**. `RIB_Y=COR_Y0+COR_CLIMB=74`(리브 접합 높이). 계단은 다리2에만, 다리1은 평면. ⚠️ App.jsx의 `COR_CLIMB` 코멘트는 "≈9.3°"로 옛 값이 남아있음(실제 18.9°) — 사소한 정리 대상.
- **사람 스케일(고정):** 눈높이 `EYE=1.6` 등. **스케일업 시 이 값들은 고정**, 건축 치수만 키운다. 걷기 = `userData{walkable:true}` 표면에 레이캐스트로 높이 맞춤.
- **조작/임시:** `FREE_WALK=true`(벽·낙하 충돌 끔 — 편집용 임시; 끄려면 false). `G`키 돔↔데이터 그래프 뷰. **시작 위치 = 방(원점)**(`ROOM_CX, ROOM_FLOOR_Y+1.6, 0`), +x(리브) 바라봄.

## 4. 요소 인벤토리 (코드명 · 상태)
| 코드 | 상태 |
|---|---|
| `rOf` / `ribCenter` | ✅ 곡선 헬퍼 |
| `DomeRibs` | ✅ 단일 속성 실체(불투명 리브 72) |
| `Ground` / `Apex` | ✅ |
| `RibStair` | ✅ φ=0 리브 *안* 나선 + 중앙 폴 |
| `LandingPad` / `StraightFlight` / `Terrace` | ✅ 나선→참→직선계단→테라스 |
| `DefAxiomRoom` | ✅ 지상 납작 돔(원점)+내벽 나선+오큘러스+착지 디스크+빛우물 실린더. 경로 관통·라벨 미완 |
| `Corridor` | ✅ **거대 원기둥(r84)+빗면천장+창±2 + 박스 연결부**(방↔원기둥, `COR_CYL_X0`로 분리). 옛 테이퍼·깔때기 폐기. **구조 확정** |
| `Stairs` | ✅ 다리2 계단(속찬 얇은 칸; tread/riser 트릭 아님) |
| (Corridor 내부) 평면 다리·원형 플랫폼·받침 기둥 | ✅ |
| `FirstPersonControls` | ✅ `FREE_WALK` 임시 켜짐 |
| `GraphScaffold` / `ethica1.js` | ✅ 점검 도구; 1~8 데이터, 9~15 미입력 |
| ~~`ClimbShaft`·`Pillars`·`Walkway`·`SmallLandingPad`~~ | ❌ **이미 제거됨**(코드에 없음) |

## 5. 정리 배치·표현 현황 (PROPOSITION MAPPING — 2026.07.02 진단)
> **"정리를 어디서 만나는가(집)"와 "무엇을 봐서 이해하는가(표현 매체)"는 별개다.** 1p2는 통로에서 *읽지만* 논증은 창 밖 *리브*가 나른다. 1p11은 테라스에 *있으면서* 무한 리브를 *본다*. → **공백 정리 다수는 새 지오메트리 없이, 이미 씬에 있는 현상(리브·돔·이음새·높이)에 시선을 걸어** 표현 가능. 빈칸 메우기의 가장 경제적 길.
> ⚠️ 현 여정은 물리적으로 **1p8까지만** 표현이 설명됨. **1p9~15는 집·표현 모두 공백** — 1부의 절정(9~11: 신의 필연적 존재)+마무리(12~15)가 통째로 빔.

| 정리 | 집(여정 위치) | 표현 매체 — 현 상태 | 판정 |
|---|---|---|---|
| 정의·공리 | 지상 방 | 방에 놓인 '주어진 것들'(텍스트) | 배치 O · 텍스트(정직) |
| 1p1 실체>변용(우선성) | 통로 플랫폼 | 없음 | 배치 O · **표현 공백** |
| 1p2 다른 속성=공통점 없음 | 통로 창 뷰 | **창 밖 5리브가 서로 안 닿음(무간섭·무지지)** | ✅ **device 확정**(미구현) |
| 1p3 공통점 없음→인과 없음 | 통로(사슬 예정) | 없음(1p2 연장 여지) | 배치 △ · **공백** |
| 1p4 속성/변용으로 구분 | 통로(사슬 예정) | 없음 | 배치 △ · **공백** |
| 1p5 동일 속성 둘 이상 불가 | 나선(개념 배정) | 없음('리브 1=실체 1'이 곧 1p5) | 배치 △ · 공간이 암시 |
| 1p6 실체는 타 실체서 산출 불가 | 나선(개념 배정) | 없음 | 배치 △ · **공백** |
| 1p7 본성에 존재 속함 | 나선(개념 배정) | 없음 | 배치 △ · **공백**(무게 큼) |
| 1p8 실체는 필연적 무한 | 나선 top | 끝없는 높이=무한(스타일) | 배치 O · 스타일(부분) |
| 1p9 실재성↑→속성↑ | **없음** | 없음 | **미배치·공백** |
| 1p10 각 속성은 자기 통해 파악 | **없음** | 없음(1p2 유사 결) | **미배치·공백** |
| 1p11 신 필연 존재 | 테라스(오르며) | **테라스 도착 시 무한 리브 공개** | ✅ **device 확정·핵심** |
| 1p12 분할되면 파악 불가 | **없음** | 이음새 없음(스타일)·미귀속 | 미배치·스타일 암시 |
| 1p13 절대 무한 실체 불가분 | **없음** | 이음새 없는 하나의 돔(스타일)·미귀속 | 미배치·스타일 암시 |
| 1p14 신 외 실체 없음(유일) | **없음** | 돔 하나뿐/리브 한 정점 수렴·미귀속 | 미배치·스타일 암시 |
| 1p15 만물은 신 안에(내재) | 전용 자리 X | 돔 '외부 없음'(LOCKED)=공간 DNA | 스타일 있음·전용 자리 X |

**3분류:**
- **① device 확정 (2):** 1p2(창 밖 리브 무간섭), 1p11(테라스 무한 공개). 1p11은 설계의 절정(스포 공개)=논리의 절정이 같은 순간 → 거의 필연.
- **② 공간이 암시하나 특정 정리에 '못 박히지' 않음 (5):** 1p8·1p12·1p13·1p14·1p15. 돔·리브가 이미 말하는데 *귀속 장치*(텍스트·시선 유도·타이밍)가 없음. 새로 만들 것 거의 없고 **묶기만** 하면 됨.
- **③ 완전 공백 — 진짜 숙제 (6):** 1p3·1p4·1p6·1p7·1p9·1p10. 집도 device도 없음. 다수(3·4·6·9·10)는 "A이므로 B" 논리 연결 단계 → 억지 건축보다 **텍스트가 더 정직할 수 있음**(텍스트 tier는 정당한 최종 답). **1p7(존재의 필연성)만** 무게가 커 자기 건축적 순간 값어치.

**미결 포크(→ §7):** (a) **1p9~15의 '집'** — ① 테라스 흡수 / ② 별도 구간 삽입 / ③ 혼합(9~11만 별도 강한 순간, 12~15 테라스 흡수) · **Claude 추천 ③**. (b) **5~8을 나선에 둘지** — 1p5는 1~4의 *결론*이라 통로 끝(나선 입구)으로 당길 여지.

## 6. 다음 세션의 핵심 작업 (NEXT SESSION)
> ⚠️ **DESIGN.md 본문(특히 §3)은 여러 세션 뒤처져 있음. 코드 = 정본.** 다음 세션 시작 시 §3 파라미터를 실제 코드로 리드백하며 갱신 권장. (§3의 **방(`DefAxiomRoom`) 부분 — 원뿔대 빛우물·오큘러스·착지 링·내벽 나선계단 — 은 이번에 현행화함**. 나머지(통로·`RibStair` 등)는 아직 뒤처질 수 있음.)

**최근 완료:**
- **박스 통로 ↔ 원뿔대 ↔ 돔 겹침을 CSG(`three-bvh-csg`)로 정공법 해결.** 면별 클립 근사(격자 컷아웃 등)는 교선이 어긋나 폐기. `HOLLOW_SUBTRACTION`(A 표면 중 B 내부만 제거)으로: 박스 껍질−원뿔대(파고듦 제거)·원뿔대 통벽−박스(통로 구멍)·원뿔대−돔 타원체(구 겹침). **CSG는 렌더 컴포넌트별로 분리해야 함**(변수 스코프): 원뿔대 `wellCut`=`DefAxiomRoom`, 박스 `boxWallCut`/`boxCeilCut`=`Corridor`.
- **방 내벽 나선계단 전면 재설계 → 공중에 뜬 낱장 디딤판.** 옛 매끈 helix(flare/속찬 디딤판+스트링거) 폐기. 스케치대로 **중심으로 감기는 나선**, 얇은 판을 띄엄 배치. **높이 기준 배치**(칸마다 RISE 상승, `nStep=round(CLIMB/RISE)`)로 경사 급한 안쪽은 자동 촘촘.
  - **8각형(옥타곤) ↔ 원형(매끈) `T`키 A/B 비교 구현.** 둘 다 같은 튜닝 파라미터·낱장 스타일, **경로 형태만** 다름 → **사용자 결정: 원형(매끈) 채택**(기본값 `circle`), 옥타곤은 개발용 비교로 보존.
  - **파라미터 튜닝(사용자):** `ROUT=45`·`TURNS=2`·`RISE=0.35`·`BIAS=1.0`·`SLAB=0.35`·`TREAD=4.0`·`RIN=14`.
  - **★핵심 원리(계단다움 ↔ 헤드룸 ↔ 크기의 근본 상충):** 평균 경사 = `atan(총상승 / 경로길이)`. **나선이 클수록(ROUT·TURNS↑) 경로가 길어져 경사↓ → 칸 사이가 성겨짐**(계단답지 않음). 반대로 작을수록 가팔라 촘촘. 어떤 높이 프로파일로도 못 바꾸고 **오직 경로길이(ROUT/TURNS)를 줄여야** 계단이 촘촘해짐. **2바퀴에선 경로가 짧아 `bias 1.0`(고른 상승)으로도 오큘러스 전에 머리 안 걸림** → '후반 급경사' 꼼수 불필요(그게 바깥 성김의 원인이었음). ROUT 45/2바퀴/bias 1.0 = 계단다움·헤드룸·크기 균형점.

**다음 작업:**
0. **★ 다음 세션 = Claude Fable 5로 전면 검토** — 계획(운용·완결 정의)·코드(App.jsx)·설계(DESIGN.md·§5 매핑)를 Fable 모델로 리뷰. (사용자 지정. 이 세션 후 새 창에서 진행.)
1. **정리 배치·표현 매핑 마무리(§5)** — 최상위 실질 작업. 완전 공백 6개(1p3·1p4·1p6·1p7·1p9·1p10) 아이디어 + 1p9~15 '집' 결정 + 5~8 배치 확정. **개별 조각 전에 1~15 전체 배치 골격부터.**
2. **정리 텍스트(1p/1d/1a) 표시 메커니즘** — 매핑 확정 후 착수. 1p1을 플랫폼(`PLAT_X`)에 얹기부터. POC(`slice3.html`)의 CanvasTexture 패널 + 거리별 smoothstep 투명도 패턴을 R3F로 이식.
3. **별자리 시스템**(Tab 오버레이: 증명 텍스트 + 의존선 + 노드 클릭 순간이동) — 프로젝트 동기의 핵심. 정리가 공간에 놓인 뒤 의미가 삶.
4. **안 쓰는 상수 정리:** `WELL_DOOR_HALF`·`WELL_DOOR_TOP`(CSG가 문 로직 대체)·`ROOM_CLIMB_H`(dead const)·`COR_CLIMB` 옛 코멘트(≈9.3°→실제 18.9°).

## 7. 열린 / 미룬 결정
- **★ 정리 배치·표현 매핑(§5)이 최상위 미결.** 다음 실질 세션의 축: 9~15의 '집'(①/②/③, Claude 추천 ③), 5~8 배치(나선 유지 vs 1p5 통로 끝), 공백 6개 표현(건축 device vs 텍스트 tier).
- ~~막힌 깔때기 외피~~·~~테이퍼 통로~~·~~원뿔대 교체·CSG 겹침~~ → **완료: 거대 원기둥 + 박스 연결부 + CSG 정밀 절단, 구조 확정.** 남은 것: 경로 관통·진입 동선 정합.
- 통로 끝(`RIB_Y=74`) ↔ 리브 안 나선 입구 **높이 맞춤**(현재 정밀 연결 안 됨; `COR_CLIMB` 올려 RIB_Y가 54→74로 이동했으니 이 값 기준으로 맞출 것).
- 길 계단 경사/폭 미세조정 — 현재 경사 ≈18.9°·`COR_RISE=0.43`·`COR_FLOOR_HW=2.5`, **사용자 만족**.
- 플랫폼 1개 → **1p1~1p4 사슬**(다리-플랫폼-다리…) 확장.
- **정리 텍스트(1p/1d/1a) 표시 메커니즘** — 코드에 텍스트 장치 없음. 배치·표현 매핑(§5) 확정 후 착수.
- 방 돔(r91)↔거대 원기둥(42~91) 겹침: 현재 `domeClipY`로 처리. 거슬리면 `COR_CYL_X0`↑(원기둥을 돔 밖으로)로 조절. 시작 위치는 방(원점)으로 이동 완료.
- 9~15 의존 데이터 입력(형태 먼저 — 보류).
- drei 도입 여부. `FREE_WALK` 임시(편집 끝나면 false).
- **테라스 "무한 리브" 공개 연출 실제 구현**(불투명 리브라 *여기*가 공개 지점).
- 16~36 확장 여부(욕심 목표).

## 8. 인프라 좌표
- **코드(단일 원천):** GitHub 공개 `https://github.com/Hyeondo1209/ethica-architectonics`
  - **읽기 = `git clone --depth 5`** (raw는 web_fetch·curl 모두 Fastly 옛 캐시 반환 — 검증됨; `api.github.com`은 공유 IP rate-limit). 주요 파일: `src/App.jsx` (그 외 `src/GraphScaffold.jsx`, `src/ethica1.js`)
- **이 정본:** `https://raw.githubusercontent.com/Hyeondo1209/ethica-architectonics/main/DESIGN.md` (저장소 루트)
- **연대기:** 노션 'Ethica Architectonics 프로젝트 일지' DB.
- **로컬:** VS Code · `C:\Users\김현도\ethica-architectonics` · `npm run dev` → http://localhost:5173. 코드/문서 변경 후 GitHub Desktop **Commit→Push**가 유일한 수동 단계. (**이번 세션(2026.07.02) push 대상: `DESIGN.md`만** — 설계·진단 세션이라 코드 변경 없음. `src/App.jsx`·`package.json`은 7/1 21:07 커밋으로 이미 최신.)
